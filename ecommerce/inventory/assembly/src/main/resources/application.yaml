spring:
  application:
    name: inventory-application

server:
  port: 8000

axon:
  axonserver:
    enabled: true
  serializer:
    general: jackson
    events: jackson
    messages: jackson



springdoc:
  swagger-ui:
    use-root-path: true